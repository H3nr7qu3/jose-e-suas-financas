# Define o banco que será usado: postgres ou mysql
#app.datasource.target=postgres
app.datasource.target=mysql


## Configuração para PostgreSQL
#postgres.datasource.url=jdbc:postgresql://localhost:5432/managementfinance
#postgres.datasource.username=postgres
#postgres.datasource.password=root
#postgres.datasource.driver-class-name=org.postgresql.Driver
#postgres.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Banco em uso (comentado pois não está sendo usado diretamente)

# app.datasource.target=mysql

# Configuração para MySQL (no padrão que o Spring espera)
spring.datasource.url=jdbc:mysql://localhost:3306/managementfinance?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=C@ldeir4
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
